const { TestWatcher } = require('@jest/core')
const Scooter = require('./Scooter')
const ScooterApp = require('./ScooterApp')
const User = require('./User')

describe('User', () => {
    test('has a pswd', () => {
        expect(testuserName.password).toBe("")

    test('has an age', () => {
            expect(testuserName.age).toBe()

    })
})

})